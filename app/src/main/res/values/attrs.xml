<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="DotLoader">
        <attr name="dot_radius" format="dimension" />
        <attr name="color_array" format="reference" />
        <attr name="number_of_dots" format="integer" />
    </declare-styleable>

    <declare-styleable name="CameraView">
        <!--
          Set this to true if you want the CameraView to adjust its bounds to preserve the aspect
          ratio of its camera preview.
        -->
        <attr name="android:adjustViewBounds"/>
        <!-- Direction the camera faces relative to device screen. -->
        <attr name="facing" format="enum">
            <!-- The camera device faces the opposite direction as the device's screen. -->
            <enum name="back" value="0"/>
            <!-- The camera device faces the same direction as the device's screen. -->
            <enum name="front" value="1"/>
        </attr>
        <!-- Aspect ratio of camera preview and pictures. -->
        <attr name="aspectRatio" format="string"/>
        <!-- Continuous auto focus mode. -->
        <attr name="autoFocus" format="boolean"/>
        <!-- The flash mode. -->
        <attr name="flash" format="enum">
            <!-- Flash will not be fired. -->
            <enum name="off" value="0"/>
            <!--
              Flash will always be fired during snapshot.
              The flash may also be fired during preview or auto-focus depending on the driver.
            -->
            <enum name="on" value="1"/>
            <!--
              Constant emission of light during preview, auto-focus and snapshot.
              This can also be used for video recording.
            -->
            <enum name="torch" value="2"/>
            <!--
              Flash will be fired automatically when required.
              The flash may be fired during preview, auto-focus, or snapshot depending on the
              driver.
            -->
            <enum name="auto" value="3"/>
            <!--
              Flash will be fired in red-eye reduction mode.
            -->
            <enum name="redEye" value="4"/>
        </attr>
    </declare-styleable>


    <declare-styleable name="FabSpeedDial">
        <!-- Compulsory attributes -->
        <attr name="fabMenu" format="reference" />
        <attr name="fabGravity">
            <flag name="bottom_end" value="0x00" />
            <flag name="bottom_start" value="0x01" />
            <flag name="top_end" value="0x02" />
            <flag name="top_start" value="0x03" />
        </attr>

        <!-- Optional attributes -->
        <attr name="fabDrawable" format="color|reference" />
        <attr name="fabDrawableTint" format="color|reference" />
        <attr name="fabBackgroundTint" format="color|reference" />

        <attr name="miniFabBackgroundTint" format="color|reference" />
        <attr name="miniFabBackgroundTintList" format="reference" />
        <attr name="miniFabDrawableTint" format="color|reference" />
        <attr name="miniFabTitleTextColor" format="color|reference" />
        <attr name="miniFabTitleTextColorList" format="reference" />

        <attr name="touchGuard" format="boolean" />
        <attr name="touchGuardDrawable" format="color|reference" />

        <attr name="miniFabTitleBackgroundTint" format="color|reference"/>
        <attr name="miniFabTitlesEnabled" format="boolean"/>


    </declare-styleable>

    <declare-styleable name="CircularImageView">
        <attr name="civ_border" format="boolean"/>
        <attr name="civ_border_width" format="dimension"/>
        <attr name="civ_border_color" format="color"/>
        <attr name="civ_shadow" format="boolean"/>
        <attr name="civ_shadow_color" format="color"/>
        <attr name="civ_shadow_radius" format="float"/>
    </declare-styleable>

    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width_new" format="dimension" />
        <attr name="civ_border_color_new" format="color" />
        <attr name="civ_border_overlay" format="boolean" />
        <attr name="civ_fill_color" format="color" />
    </declare-styleable>





</resources>